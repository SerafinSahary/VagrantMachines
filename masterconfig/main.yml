---
### Main config for Ubuntu 18.04 LTS server by Ser@fin

- hosts: all
  remote_user: root
  become: yes
  vars:
  handlers:
  - import_tasks: base-handlers.yml
  tasks:
  - import_tasks: base.yml

# vi: set expandtab:
